# NivXForge — frontend namespace (reserved, empty)

Phase 0 does not introduce any user-facing UI. This directory exists
only to reserve the isolation boundary described in
`/app/memory/NORTH_STAR.md §7` (`/frontend/src/nivxforge/`).

Do not add components here without an entry in
`/app/memory/IMPLEMENTATION_ROADMAP.md §3`.
