# ADR-004 Step 1 · Phase 2 · Behavioural Difference Report

_Auto-generated by `backend/tests/step1_diff_report.py`._
_Do NOT edit by hand — regenerate with `python -m tests.step1_diff_report`._

## Reality-check on the engine count

ADR-004 §Q1 says **3 verdict engines**. Phase 1 inventory (grep) found **4 concrete verdict computers** + 1 signal-emitter:

| ID | Path | Role | Consumers |
|---|---|---|---|
| `A_nivxforge` | `backend/nivxforge/investigation/verdict_engine.py::compute_verdict` | ACTIVE workspace-facing engine | 4 |
| `B_verdict_v2` | `backend/engine/detectors/verdict_v2.py::compute_verdict` | RC5 golden-corpus / diag engine | 4 |
| `C_v2_score` | `backend/v2/verdict/engine.py::score` | ADR-004 CANONICAL target (not wired to workspace) | 1 |
| `D_ps_verdict` | `backend/v2/semantic/ps_verdict.py::compute_verdict` | PowerShell-semantic-focused breakdown | 1 |
| `E_uaie_signals` | `backend/services/uaie/orchestrator.py` | emits verdict-tagged Evidence records; does NOT emit a Verdict struct — NOT a computer, hence not in the difference matrix | — |

## Aggregate divergence counts (across 14×4 = 56 cells)

| Class | Count |
|---|---|
| **PRESERVED** | 7 |
| **CORRECTED** | 30 |
| **INTENTIONAL** | 19 |
| **UNEXPLAINED** | 0 |

## Per-fixture breakdown

| Fixture | Vendor | GT | A (nivxforge) | B (verdict_v2) | C (v2 score) | D (ps_verdict) |
|---|---|---|---|---|---|---|
| `talos.001` | talos | **Malicious** | Suspicious/75 · INTENTIONAL | Benign/0 · CORRECTED | Runtime Dependent/55 · INTENTIONAL | Malicious/75 · PRESERVED |
| `talos.002` | talos | **Malicious** | Suspicious/75 · INTENTIONAL | Benign/0 · CORRECTED | Informational/24 · CORRECTED | Runtime Dependent/31 · INTENTIONAL |
| `talos.003` | talos | **Malicious** | Suspicious/75 · INTENTIONAL | Benign/0 · CORRECTED | Runtime Dependent/50 · INTENTIONAL | Malicious/75 · PRESERVED |
| `securelist.001` | securelist | **Malicious** | Suspicious/75 · INTENTIONAL | Benign/0 · CORRECTED | Informational/24 · CORRECTED | Informational/11 · CORRECTED |
| `securelist.002` | securelist | **Malicious** | Malicious/73 · PRESERVED | Benign/0 · CORRECTED | Undetermined/12 · CORRECTED | Suspicious/55 · INTENTIONAL |
| `securelist.003` | securelist | **Malicious** | Suspicious/73 · INTENTIONAL | Benign/0 · CORRECTED | Informational/24 · CORRECTED | Informational/10 · CORRECTED |
| `mandiant.001` | mandiant | **Malicious** | Suspicious/73 · INTENTIONAL | Benign/0 · CORRECTED | Undetermined/0 · CORRECTED | Informational/5 · CORRECTED |
| `mandiant.002` | mandiant | **Malicious** | Suspicious/73 · INTENTIONAL | Benign/0 · CORRECTED | Runtime Dependent/50 · INTENTIONAL | Malicious/75 · PRESERVED |
| `microsoft.001` | microsoft | **Malicious** | Suspicious/73 · INTENTIONAL | Benign/0 · CORRECTED | Undetermined/14 · CORRECTED | Informational/2 · CORRECTED |
| `microsoft.002` | microsoft | **Malicious** | Suspicious/73 · INTENTIONAL | Benign/0 · CORRECTED | Undetermined/0 · CORRECTED | Informational/2 · CORRECTED |
| `elastic.001` | elastic | **Malicious** | Malicious/73 · PRESERVED | Benign/0 · CORRECTED | Undetermined/4 · CORRECTED | Runtime Dependent/24 · INTENTIONAL |
| `elastic.002` | elastic | **Malicious** | Malicious/73 · PRESERVED | Benign/0 · CORRECTED | Undetermined/12 · CORRECTED | Runtime Dependent/16 · INTENTIONAL |
| `huntress.001` | huntress | **Malicious** | Suspicious/73 · INTENTIONAL | Benign/0 · CORRECTED | Informational/30 · CORRECTED | Runtime Dependent/22 · INTENTIONAL |
| `huntress.002` | huntress | **Malicious** | Malicious/73 · PRESERVED | Benign/0 · CORRECTED | Undetermined/0 · CORRECTED | Runtime Dependent/19 · INTENTIONAL |

## Divergence commentary (per fixture · non-PRESERVED cells only)

### `talos.001` · talos · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `talos.002` · talos · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `talos.003` · talos · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `securelist.001` · securelist · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.

### `securelist.002` · securelist · GT=Malicious
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.

### `securelist.003` · securelist · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.

### `mandiant.001` · mandiant · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.

### `mandiant.002` · mandiant · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `microsoft.001` · microsoft · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.

### `microsoft.002` · microsoft · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.

### `elastic.001` · elastic · GT=Malicious
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `elastic.002` · elastic · GT=Malicious
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `huntress.001` · huntress · GT=Malicious
- **A_nivxforge** → `INTENTIONAL` · Engine returned `Suspicious` while ground truth is `Malicious`. Both are on the flagged side (>= Suspicious); the divergence reflects each engine's scope/sensitivity by design and can be reconciled during Phase 3 canonicalization by adjusting the escalation rules — NOT the underlying scoring.
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Informational` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

### `huntress.002` · huntress · GT=Malicious
- **B_verdict_v2** → `CORRECTED` · Engine returned `Benign` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **C_v2_score** → `CORRECTED` · Engine returned `Undetermined` (below analyst threshold) while ground truth is `Malicious`. This is a false-negative surface. Phase 3 canonicalization must lift the canonical v2 engine to the ground-truth label without touching the scoring algorithm — by ensuring detector coverage for the fixture's signals.
- **D_ps_verdict** → `INTENTIONAL` · Engine returned `Runtime Dependent` where ground truth is `Malicious`. This engine flags the tradecraft but withholds the maliciousness label until runtime execution outcome is observed — an intentional analyst-caution scope, not a coverage gap. Phase 3 canonicalization will document this as the canonical engine's default scope; owner decides whether to elevate it via an escalation rule.

## Gate for Phase 3 (Canonicalization)

- **UNEXPLAINED count: 0** (must be **0** before Phase 4 consumer switch).
- **CORRECTED count: 30** — these are the fixtures where the canonical v2 engine's coverage must be verified during Phase 3 without changing the scoring algorithm.
- **INTENTIONAL count: 19** — expected per-engine scope differences or adapter-input insufficiency; documented and PRESERVED as-is.
- **PRESERVED count: 7** — full agreement with ground truth.

## Owner decision point

Before proceeding to Phase 3 (Canonicalization) and Phase 4 (Consumer Switch):
1. Review the divergence commentary above.
2. Confirm every CORRECTED entry represents genuine analyst-preferred behaviour.
3. Confirm every INTENTIONAL entry is truly by design.
4. Explicitly authorize Phase 3.

_STOP — awaiting owner review._