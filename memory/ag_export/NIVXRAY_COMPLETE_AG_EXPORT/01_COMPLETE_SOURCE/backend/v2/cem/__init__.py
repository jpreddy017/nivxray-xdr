"""NivXRay v2 · Canonical Event Model (CEM) versioned namespace.

Per GOVERNANCE.md §6 (Versioned CEM). Each subpackage holds a
frozen schema version. Never mutate an existing version — only add
a new one.
"""
