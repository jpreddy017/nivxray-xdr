"""Placeholder — normalizers/ package (v1.6.0).

Each file re-implements one syntax normalizer:
  powershell.py — [char]+, -join, -f, Split, Reverse, IEX cradle
  cmd.py        — ^ escape, %VAR%, !VAR!, delayed expansion
  javascript.py — String.fromCharCode, unescape, eval unwrap
  vbs.py        — CreateObject, Wscript.Shell reconstruction

Placeholder file — real implementations land in Session 2.
"""
