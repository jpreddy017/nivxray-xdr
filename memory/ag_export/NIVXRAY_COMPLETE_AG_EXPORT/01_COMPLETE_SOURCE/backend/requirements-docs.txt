# NivXRay — minimal deps for the docs-export CI job.
#
# Used by `.github/workflows/docs-screenshots.yml` so we don't pull the
# full runtime (emergentintegrations, capstone, cryptography, motor, …)
# into a GitHub Actions runner. The docs pipeline only needs:
#   - YAML config parsing               → pyyaml
#   - PDF generation (reportlab + SVG)  → reportlab, svglib, pillow
#   - HTML / DOCX exports               → markdown, python-docx
#   - PDF text extraction (sync-url)    → pypdf
#   - Sparse RAG for "Explain this"     → rank-bm25
#   - Playwright screenshot capture     → playwright
#   - Auth + HTTP calls in capture      → httpx, requests
#
# Versions mirror `requirements.txt` so preview + CI + prod stay aligned.
pyyaml==6.0.3
reportlab==4.5.1
svglib==2.0.2
markdown==3.10.2
python-docx==1.1.2
pypdf==6.14.2
pillow==12.3.0
rank-bm25==0.2.2
playwright==1.48.0
httpx==0.28.1
requests==2.32.3
