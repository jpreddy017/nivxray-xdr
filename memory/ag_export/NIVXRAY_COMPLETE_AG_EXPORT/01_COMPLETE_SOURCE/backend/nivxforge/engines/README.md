# NivXForge engines — intentionally empty in Phase 0

This directory holds the `Engine` Protocol only. No engine
implementations exist yet.

Engines enter this tree ONLY after the entry gate in
`/app/memory/IMPLEMENTATION_ROADMAP.md §1` is satisfied for the
specific capability the engine embodies.

Do not populate this directory with speculative engines. Doing so
violates the Charter Principle P-C (build from repeated patterns,
never isolated examples).
