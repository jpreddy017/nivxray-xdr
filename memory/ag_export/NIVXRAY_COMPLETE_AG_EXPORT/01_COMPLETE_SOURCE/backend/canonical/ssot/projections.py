"""Empty projection scaffolding — Phase 4 populates.

Phase 2 declares the projection tier's SHAPE only. Projectors that read
the authoritative tier and emit these projections arrive in Phase 4.
Phase 2 tests assert the projection buckets remain empty.
"""

# Intentionally empty in Phase 2. See canonical/ssot/models.py for the
# projection dataclasses (ActivityProjection, IOCProjection, ...).
