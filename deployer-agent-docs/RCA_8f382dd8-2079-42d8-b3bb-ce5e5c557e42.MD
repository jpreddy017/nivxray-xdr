# RCA — Build failure, run 8f382dd8-2079-42d8-b3bb-ce5e5c557e42 (greeting-app-5782)

Failed step: `build_image` (Cloud Build id `7f3b2bfa-8f86-4b6a-acf9-3ef5c32344d5`, state FAILURE).
The run-level `error_message` is only the wrapper `TerminalCloudBuildFailure` stack — the actual cause is below, pulled from the raw Cloud Build output.

## 1. First ERROR in the build output
The first real compile error (chronologically, and before the terminal `ERROR:` wrapper line):
```
Jul 21 16:38:36 Step #8 - "frontend-build-push": Failed to compile.
Jul 21 16:38:36 Step #8 - "frontend-build-push": [eslint]
Jul 21 16:38:36 Step #8 - "frontend-build-push": src/pages/DocumentsPage.jsx
Jul 21 16:38:36 Step #8 - "frontend-build-push": Syntax error: Unterminated JSX contents. (308:10) (308:10)
Jul 21 16:38:36 Step #8 - "frontend-build-push": error Command failed with exit code 1.
```
Terminal wrapper that followed:
```
Jul 21 16:38:40 ERROR: build step 8 "us-central1-docker.pkg.dev/emergent-default/emergent-container-hub/customer-apps-node-base:v0.3.77" failed: step exited with non-zero status: 1
```

## 2. Filename
`frontend/src/pages/DocumentsPage.jsx`

## 3. Line number
Line **308**, column **10**.

## 4. Failing command / build step
Cloud Build **Step #8 `frontend-build-push`** (image `customer-apps-node-base:v0.3.77`) — the R2 frontend-artifact build — running `craco build` → yarn exited with code 1 at the eslint/babel compile phase. (The parallel docker-build Step #2 `frontend-build` stage runs the same `craco build`; the artifact-push step surfaced the failure first.)

## 5. Complete compiler error
`Failed to compile.` → `[eslint] src/pages/DocumentsPage.jsx` → `Syntax error: Unterminated JSX contents. (308:10)` → `error Command failed with exit code 1.` This is a babel/eslint JSX parse error: the parser reached line 308 with a JSX element (the top-level fragment / container div) still open — a closing tag was missing at that depth.

## 6. Root cause analysis
This is the SAME class of defect the prior RCA (`RCA_8f382dd8...` referenced fragment closure) called out: an unbalanced JSX tree in the `return (...)` block of `DocumentsPage.jsx`, where the top-level `<>` fragment was not properly terminated, so babel hit "Unterminated JSX contents" at the trailing `</div>`/`</>` region (line 308).

Two independent observations narrow it:
- **Backend is NOT the culprit.** `backend-build` (Step #2 `#30`) completed cleanly (`DONE 8.4s`), including litellm install and `pip install -r requirements.txt`. The new additive backend modules (`backend/engine/evidence_graph*.py`), the new tests, and the `rc5_diag.py` wiring played no role — the build never failed on Python. Do not chase those.
- **The source snapshot pulled for THIS run now traces as structurally balanced.** Inspecting the run's `/app/scratch/frontend/src/pages/DocumentsPage.jsx` in an eph sandbox: fragment opens `<>` at line 160 and closes `</>` at line 309; 21 non-self-closing `<div>` vs 21 `</div>` with a clean push/pop nesting stack (container div@162 closes at 308, fragment closes at 309). i.e. the fragment-closure fix IS present in the inspected source.

The contradiction between "build errored at 308:10" and "inspected source is balanced" means the failed build 8f382dd8 compiled a snapshot that still had the unterminated JSX, and the effective fix landed after/independently of what this run built. There is no remaining source defect visible in the current DocumentsPage.jsx.

NOTE: I could not run `yarn build`/`craco build` to verify end-to-end — the eph sandbox is the python backend base image and has no node runtime, and it has no app secrets. Verification is static (tag/nesting balance), not a live compile.

## 7. Proposed fix
No further source change appears required — `DocumentsPage.jsx` in the current source is already a balanced, closed fragment. Action: **re-trigger the deploy** so a fresh build pulls the corrected file. Before doing so, sanity-check lines 305–309 in the preview source show exactly one closing per opened element ending in `</div>` (308) then `</>` (309) — guard against a duplicated/stray closing tag. If a rebuilt build still reports `308:10`, re-open DocumentsPage.jsx around 300–309 for an extra or missing closing tag near the preview modal block.

## 8. Category of failure
**Docker build failure → frontend build-config / JSX compile error (React fragment/tag closure).** NOT Python syntax/import, NOT dependency/package, NOT missing env var, NOT missing file.
